{
  "name": "weave",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "A cooperative-network agent framework — autonomous Claude workers coordinating as peers over a shared substrate.",
  "engines": {
    "node": ">=18.17"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test $(find src -name '*.test.ts')"
  },
  "comments": {
    "dependencies": "@anthropic-ai/claude-agent-sdk is added in the ADR-0003 Worker-adapter phase; the current code (domain/ports/in-process substrate/fake worker) needs no runtime deps."
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^20.0.0",
    "tsx": "^4.16.0"
  }
}
